import 'package:flutter/material.dart';

class TrackPageWidget extends StatelessWidget {
  const TrackPageWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Track Page Widget"));
  }
}
